tasks.register('enableAndroidBuild') {
    doLast {
        project.setProperty("android", true)
    }
}

task androidLint(dependsOn: [enableAndroidBuild], type: GradleBuild) {
    tasks = ['lintDebug']
}
